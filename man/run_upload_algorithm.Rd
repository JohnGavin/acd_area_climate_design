% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload_algorithm.R
\name{run_upload_algorithm}
\alias{run_upload_algorithm}
\title{Run the upload-page address-matching algorithm in R}
\usage{
run_upload_algorithm(input_df, addresses = NULL, fuzzy_max_distance = 20L)
}
\arguments{
\item{input_df}{data.frame with columns PLZ, Strasse, HausNr (and optional
Stiege_RH, ObjektNr). Column names must already be normalised (no trailing
dots or slashes — rename before passing in).}

\item{addresses}{Either a data.frame (the address table) or a character path
to an ADRESSENOGD parquet file. Defaults to
\code{system.file("extdata", "addresses_2026-05-02.parquet", package = "acd")}.}

\item{fuzzy_max_distance}{Integer. Maximum numeric house-number distance for
fuzzy fallback (mirrors JS constant \code{FUZZY_MAX_DISTANCE = 20}).
Default 20.}
}
\value{
data.frame with original columns plus:
\describe{
\item{match_quality}{One of \code{"exact"}, \code{"ambiguous_multi"},
\code{"fuzzy_nearest_house"}, \code{"split_both"}, \code{"split_partial"},
\code{"unmatched"}.}
\item{ACD}{Primary ACD code, or \code{NA} for unmatched rows.}
\item{acd_list}{Character; comma-separated list of all ACD codes for
multi-unit buildings; same as ACD for single-unit.}
\item{acd_count}{Integer; number of ACD codes at this address.}
\item{house_distance}{Integer; numeric house-number distance from the
nearest registered house (only for \code{fuzzy_nearest_house}; \code{NA}
otherwise).}
\item{n_acd_candidates}{Alias for \code{acd_count} kept for API
compatibility.}
}
}
\description{
Mirrors the JS algorithm in \code{vignettes/articles/upload.qmd} so that test
results in R match what the deployed page produces. Used by snapshot
regression tests.
}
\details{
\strong{Implementation status (v0):}
\itemize{
\item Step 1: Normalise input (trim, lowercase street, normalise HausNr). DONE.
\item Step 2: Exact join on (PLZ, street_norm, house_norm). DONE.
\itemize{
\item Step 2a: multi-unit (acd_count > 1) flagged as \code{ambiguous_multi}. DONE.
}
\item Step 3: Fuzzy nearest-house fallback for unmatched simple rows. DONE.
\item Step 4: Composite-street splitter (Strasse contains "/"). DONE.
\item Step 5: multi-unit ARRAY_AGG (acd_list). DONE.
\item Step 6: REST API fallback (Fix C). NOT IMPLEMENTED — requires live network.
TODO: implement optional REST fallback in a follow-up PR.
}
}
